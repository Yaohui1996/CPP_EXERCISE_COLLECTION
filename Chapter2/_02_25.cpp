#include <iostream>

int main() {
    int *ip, i, &r = i;
    int j, *jp = 0;
    int *kp, kp2;
    /*
     * ip 指向int型变量的指针
     * i int型变量
     * r int型变量的引用
     * j int型变量
     * jp jp被初始化，但没指向任何对象
     * kp 指向int型变量的指针
     * kp2 int型变量
     */
    return 0;
}
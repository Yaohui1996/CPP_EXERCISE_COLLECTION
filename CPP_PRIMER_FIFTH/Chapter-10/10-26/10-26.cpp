// 练习10.26：解释三种插入迭代器的不同之处。
// 解答：三种迭代器分别为back_inserter、front_inserter、inserter。
// back_inserter指向容器的尾部，对其赋值调用对应的push_back()函数；
// front_inserter指向容器的头部，对其赋值调用对应的push_front()函数；
// inserter则调用对应的insert()函数。

int main()
{
    return 0;
}
